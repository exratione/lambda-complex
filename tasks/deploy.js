/**
 * @fileOverview Grunt task deploy.
 *
 * Build and deploy a Lambda Complex application as a CloudFormation stack.
 */

// Local.
var task = require('../lib/grunt/tasks/deploy');

module.exports = task
